# Server Configuration
PORT=3000
NODE_ENV=development

# Groq API Configuration (Required)
# Get your API key from https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# JWT Configuration (Required for authentication)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# CORS Configuration (Comma-separated list of allowed origins)
# For development, you can use '*' to allow all origins
# For production, specify your domain(s) like: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=*

# Logging Level (optional, defaults to 'debug')
LOG_LEVEL=debug

# WebRTC Configuration (optional, for future use)
# STUN_SERVERS=stun:stun1.l.google.com:19302,stun:stun2.l.google.com:19302
# TURN_SERVERS=turn:your-turn-server.com:3478
# TURN_USERNAME=your_turn_username
# TURN_CREDENTIAL=your_turn_credential

# Database Configuration (if applicable in the future)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=scope_db
# DB_USER=postgres
# DB_PASSWORD=your_db_password

# Email Configuration (if applicable in the future)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_email_password
# EMAIL_FROM=noreply@example.com
